# Finnhub API key — required for the /api/quotes market ticker endpoint.
# Get a free key at https://finnhub.io/
# Set this in your Vercel project environment variables (or local .env for vercel dev).
FINNHUB_API_KEY=your_finnhub_api_key_here
